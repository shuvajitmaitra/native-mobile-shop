import { View, Text, ScrollView } from 'react-native'
import React from 'react'

const Cart = () => {
  return (
    <ScrollView>
      <Text>This is cart Page</Text>
    </ScrollView>
  )
}

export default Cart